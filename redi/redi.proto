
message FloatVec {
  repeated float values = 1 [ packed = true ];
}

message Uint32Vec {
  repeated uint32 values = 1 [ packed = true ];
}

message NetworkProto {
  required int32 num_layers = 1;

  repeated Uint32Vec indices = 2;
  repeated FloatVec weights = 3;
  repeated FloatVec biases = 4;
};