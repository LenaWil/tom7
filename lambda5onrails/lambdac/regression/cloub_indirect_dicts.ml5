unit
    extern val js.alert : string -> unit @ home
    extern bytecode world server
    extern val home ~ home addr
    extern val version : unit -> string @ home

    fun goswap (a) =
        let in
            (* js.alert (from home get version ()); *)
            js.alert (version ());
            (a)
        end

in
    export val pair = goswap
end
