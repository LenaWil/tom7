unit
    extern val js.alert : string -> unit @ home
    extern world server
    extern val server ~ server addr
    extern type version
    extern val vtos ~ version -> string
    extern val version : unit -> version @ server

    (* test... *)
    extern val (a, b) polyswap : a * b -> b * a @ server

    do js.alert "Hello, World 2.0!"

    fun goswap (a, b) =
	let in
	    js.alert (from server get vtos (version ()));
	    (b, a)
	end
in
    export val x = 3
    export type abstracty = int
end
