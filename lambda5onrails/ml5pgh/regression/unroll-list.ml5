unit
    datatype a list = nil | :: of a * list
    infixr ::

    fun list-map () =
        let
            fun m nil = 0
              | m (_ :: _) = 0
        in
            m
        end

    (* val template-cache = ref nil *)
        
    do (list-map () nil)
in
end
